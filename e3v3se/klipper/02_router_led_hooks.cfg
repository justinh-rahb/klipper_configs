# ==== LED HOOKS (Router Bridge) ====
# Replicates 04_disco_hooks.cfg but uses bridge macros

[delayed_gcode startup_status_light]
initial_duration: 3.0
gcode:
    #DISCO_COMPLETE
    DISCO_IDLE
    #DISCO_BREATHING
